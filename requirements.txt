# Core server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Computer vision & ML
opencv-python>=4.9.0.80
mediapipe>=0.10.14
numpy>=1.26.0
scipy>=1.13.0

# Config
pyyaml>=6.0.1

# Torch (CPU fallback; install GPU version separately via pip install torch --index-url ...)
torch>=2.2.0
